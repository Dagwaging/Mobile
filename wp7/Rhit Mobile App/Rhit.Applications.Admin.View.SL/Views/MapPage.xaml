<navigation:Page x:Class="Rhit.Applications.View.Views.MapPage" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:navigation="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Navigation"
    xmlns:map="clr-namespace:Microsoft.Maps.MapControl;assembly=Microsoft.Maps.MapControl"
    xmlns:navi="clr-namespace:Microsoft.Maps.MapControl.Navigation;assembly=Microsoft.Maps.MapControl"
    xmlns:controls="clr-namespace:Rhit.Applications.View.Controls"
    xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"
    xmlns:cmds="clr-namespace:Rhit.Applications.Mvvm.Commands;assembly=Rhit.Applications.Mvvm.SL"

    mc:Ignorable="d" d:DesignWidth="640" d:DesignHeight="480"
    Title="MapPage"
    Style="{StaticResource PageStyle}">
    
    <navigation:Page.Resources>
        <DataTemplate x:Name="PolygonItemTemplate">
            <map:MapPolygon Tag="{Binding Id}" MouseLeftButtonUp="MapPolygon_Click" Locations="{Binding Locations}"
                            Fill="{StaticResource PolygonFillColorBrush}" Stroke="White" StrokeThickness="1"/>
        </DataTemplate>

        <Style TargetType="ListBox" x:Key="NavigationBarList">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding Label}" Foreground="White" FontWeight="Bold" Margin="5,0"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox" x:Key="BehaviorList">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="Bold" Margin="5,0"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="toolkit:Expander" x:Key="ExpanderStyle">
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="ExpandDirection" Value="Left"/>
            <Setter Property="MinHeight" Value="100"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="Gray" Opacity="0.7"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="TextBlock" x:Key="ExpanderHeaderStyle">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Margin" Value="-7,20,-7,0"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform Angle="90"/>
                </Setter.Value>
            </Setter>
        </Style>

        <StackPanel Orientation="Horizontal" x:Name="NavigationBarItems">
            <ListBox Style="{StaticResource NavigationBarList}" ItemsSource="{Binding Map.Modes}" 
                     SelectedValue="{Binding Map.CurrentMode, Mode=TwoWay}" />
            <navi:CommandSeparator/>
            <ListBox Style="{StaticResource NavigationBarList}" ItemsSource="{Binding Map.Sources}"
                     SelectedValue="{Binding Map.CurrentSource, Mode=TwoWay}" />
        </StackPanel>

    </navigation:Page.Resources>

    <Grid x:Name="LayoutRoot" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Row="0" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Style="{StaticResource HeaderTextStyle}" Text="Map View" VerticalAlignment="Center" Margin="20,5,5,5" Grid.Column="0"/>
            <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
                <ListBox Style="{StaticResource BehaviorList}" ItemsSource="{Binding Behaviors}" SelectedValue="{Binding Behavior, Mode=TwoWay}"/> 
            </StackPanel>
        </Grid>

        <Grid x:Name="LayoutMain" Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="0"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="{Binding Image.Loaded, Converter={StaticResource GridLengthConverter}}"/>
            </Grid.ColumnDefinitions>

            <map:Map Name="MyMap" Grid.Row="0" Grid.Column="0" CredentialsProvider="AthZ1tu5ROM0PUWcIYFSxC1oQALFR-g0aoFIuL9tlbeGJ9Z6qKIRYoB_jGpct8Yu">
                <map:MapLayer Name="CornersLayer"/>
                <map:MapLayer>
                    <map:MapItemsControl ItemsSource="{Binding Locations.Buildings}" ItemTemplate="{StaticResource PolygonItemTemplate}" Visibility="{Binding Behavior.AreBuildingsVisible, Mode=TwoWay, Converter={StaticResource VisibilityConverter}}"/>
                    <map:MapItemsControl ItemsSource="{Binding Locations.All}" ItemTemplate="{StaticResource PushpinItemTemplate}" Visibility="{Binding Behavior.AreLocationsVisible, Mode=TwoWay, Converter={StaticResource VisibilityConverter}}"/>
                    <map:MapItemsControl ItemsSource="{Binding Locations.Corners}" ItemTemplate="{StaticResource PushpinItemTemplate2}"/>
                </map:MapLayer>
                <controls:DraggablePushpin 
                    Content="{Binding Path=Locations.CurrentLocation, Converter={StaticResource LocationNameConverter}, Mode=TwoWay}"
                    Location="{Binding Locations.CurrentLocation, Converter={StaticResource LocationCenterConverter}, Mode=TwoWay}"/>
            </map:Map>

            <sdk:GridSplitter Grid.Column="1" Width="8" HorizontalAlignment="Left" VerticalAlignment="Stretch" ShowsPreview="True"/>

            <ScrollViewer Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Margin="5,0,0,0">
                <controls:ImageViewer CurrentImage="{Binding Image.Bitmap}" MouseLeftButtonUp="ImageViewer_MouseLeftButtonUp" PointList="{Binding Image.ImagePoints}"/>
            </ScrollViewer>

            <ScrollViewer Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <StackPanel Name="VerticalViewer"/>
            </ScrollViewer>
        </Grid>

        <Grid Grid.Row="1" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <toolkit:Expander Grid.Column="1" Style="{StaticResource ExpanderStyle}" >
                <toolkit:Expander.Header>
                    <TextBlock Text="Tools" Style="{StaticResource ExpanderHeaderStyle}"/>
                </toolkit:Expander.Header>
                <StackPanel HorizontalAlignment="Right">
                    <Button cmds:CommandManager.Command="{Binding GotoRhitCommand}" cmds:CommandManager.CommandEventName="Click" Margin="5,5,5,0" Content="Goto RHIT" HorizontalAlignment="Center"/>

                    <Line Margin="0,5" Stroke="DarkGray"  StrokeThickness="2" Stretch="Fill" X1="0" X2="1"/>

                    <!--Image Options-->
                    <Button cmds:CommandManager.Command="{Binding Image.LoadCommand}" cmds:CommandManager.CommandEventName="Click" Margin="5,5,5,0" Content="Load Image" HorizontalAlignment="Center"/>

                    <StackPanel Margin="5,15,5,0" Visibility="{Binding Image.Loaded, Converter={StaticResource VisibilityConverter}}">
                        <TextBlock Text="Map/Image Arrangment:" Margin="0,5,5,0"/>
                        <RadioButton Margin="5,5,5,0"  GroupName="ViewArrangment" Content="Horizontal" IsChecked="True"/>
                        <RadioButton Margin="5,5,5,0"  GroupName="ViewArrangment" Content="Vertical"/>
                        <Button cmds:CommandManager.Command="{Binding Image.CloseCommand}" cmds:CommandManager.CommandEventName="Click" Margin="5,5,5,0" Content="Close Image"/>
                    </StackPanel>

                    <Line Margin="0,5" Stroke="DarkGray"  StrokeThickness="2" Stretch="Fill" X1="0" X2="1"/>

                    <!--Buildings Options-->
                    <StackPanel Visibility="{Binding AreBuildingOptionsVisible, Converter={StaticResource VisibilityConverter}}">
                        <TextBlock HorizontalAlignment="Center" FontWeight="Bold" Text="{Binding Locations.CurrentLocation, Mode=TwoWay, Converter={StaticResource LocationNameConverter}, FallbackValue=Current_Location}"/>
                        <StackPanel Visibility="{Binding Locations.CurrentLocation, Converter={StaticResource OVisibilityConverter}}">
                            <Button Content="Move Corners" Margin="5" HorizontalAlignment="Center" cmds:CommandManager.Command="{Binding Behavior.ChangeCornersCommand}" cmds:CommandManager.CommandEventName="Click" Visibility="{Binding Behavior.AreSaveCancelVisible, Converter={StaticResource ReverseVisibilityConverter}}"/>
                            <Button Content="Add/Remove Corners" Margin="5" HorizontalAlignment="Center" cmds:CommandManager.Command="{Binding Behavior.AddCornersCommand}" cmds:CommandManager.CommandEventName="Click" Visibility="{Binding Behavior.AreSaveCancelVisible, Converter={StaticResource ReverseVisibilityConverter}}"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Save" Margin="5" HorizontalAlignment="Center" cmds:CommandManager.Command="{Binding Behavior.SaveCommand}" cmds:CommandManager.CommandEventName="Click" Visibility="{Binding Behavior.AreSaveCancelVisible, Converter={StaticResource VisibilityConverter}}"/>
                                <Button Content="Cancel" Margin="5" HorizontalAlignment="Center" cmds:CommandManager.Command="{Binding Behavior.CancelCommand}" cmds:CommandManager.CommandEventName="Click" Visibility="{Binding Behavior.AreSaveCancelVisible, Converter={StaticResource VisibilityConverter}}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </toolkit:Expander>
        </Grid>
    </Grid>

</navigation:Page>