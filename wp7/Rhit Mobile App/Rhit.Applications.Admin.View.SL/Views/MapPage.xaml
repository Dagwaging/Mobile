<navigation:Page x:Class="Rhit.Applications.View.Views.MapPage" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:navigation="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Navigation"
    xmlns:map="clr-namespace:Microsoft.Maps.MapControl;assembly=Microsoft.Maps.MapControl"
    xmlns:navi="clr-namespace:Microsoft.Maps.MapControl.Navigation;assembly=Microsoft.Maps.MapControl"
    xmlns:controls="clr-namespace:Rhit.Applications.View.Controls"
    xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"
    xmlns:cmds="clr-namespace:Rhit.Applications.Mvvm.Commands;assembly=Rhit.Applications.Mvvm.SL"
                 xmlns:util="clr-namespace:Rhit.Applications.View.Utility"
                 xmlns:vm="clr-namespace:Rhit.Applications.ViewModel.Models;assembly=Rhit.Applications.Admin.ViewModel.SL"
                 xmlns:i="http://schemas.microsoft.com/expression/2010/interactions"
                 xmlns:it="http://schemas.microsoft.com/expression/2010/interactivity"
    mc:Ignorable="d" d:DesignWidth="640" d:DesignHeight="480"
    Title="MapPage"
    Style="{StaticResource PageStyle}">
    
    <navigation:Page.Resources>
        <!--TODO: Remove | It is currently overwritten in code behind-->
        <vm:MainViewModel x:Name="ViewModel" x:Key="ViewModel"/>
       
        <util:TaskContainer x:Name="TaskManager">
            <util:TaskContainer.Tasks>
                <util:Task Label="Move Building Corners" Command="{Binding Behavior.ChangeCornersCommand, ElementName=ViewModel}">
                    <util:Task.Steps>
                        <util:TaskStep Label="Move Corners"/>
                    </util:Task.Steps>
                </util:Task>
            </util:TaskContainer.Tasks>
        </util:TaskContainer>

        <DataTemplate x:Name="PolygonItemTemplate">
            <map:MapPolygon Tag="{Binding Id}" MouseLeftButtonUp="MapPolygon_Click" Locations="{Binding Locations}"
                            Fill="{StaticResource PolygonFillColorBrush}" Stroke="White" StrokeThickness="1"/>
        </DataTemplate>

        <Style TargetType="ListBox" x:Key="NavigationBarList">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding Label}" Foreground="White" FontWeight="Bold" Margin="5,0"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox" x:Key="BehaviorList">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="Bold" Margin="5,0"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="toolkit:Expander" x:Key="ExpanderStyle">
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="ExpandDirection" Value="Left"/>
            <Setter Property="MinHeight" Value="100"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="Gray" Opacity="0.7"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="TextBlock" x:Key="ExpanderHeaderStyle">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Margin" Value="-7,20,-7,0"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform Angle="90"/>
                </Setter.Value>
            </Setter>
        </Style>

        <StackPanel Orientation="Horizontal" x:Name="NavigationBarItems">
            <ListBox Style="{StaticResource NavigationBarList}" ItemsSource="{Binding Map.Modes}" 
                     SelectedValue="{Binding Map.CurrentMode, Mode=TwoWay}" />
            <navi:CommandSeparator/>
            <ListBox Style="{StaticResource NavigationBarList}" ItemsSource="{Binding Map.Sources}"
                     SelectedValue="{Binding Map.CurrentSource, Mode=TwoWay}" />
        </StackPanel>

        <DataTemplate x:Name="PushpinItemTemplate">
            <map:Pushpin Location="{Binding Center}" MouseLeftButtonUp="Pushpin_Click"/>
        </DataTemplate>

        <DataTemplate x:Name="ImagePinTemplate">
            <Canvas>
                <Ellipse Fill="Orange" Width="10" Height="10" Canvas.Left="{Binding X}" Canvas.Top="{Binding Y}">
                    <Ellipse.RenderTransform>
                        <TranslateTransform X="-5" Y="-5"/>
                    </Ellipse.RenderTransform>
                    <it:Interaction.Behaviors>
                        <i:MouseDragElementBehavior/>
                    </it:Interaction.Behaviors>
                </Ellipse>
            </Canvas>
        </DataTemplate>
    </navigation:Page.Resources>

    <Grid x:Name="LayoutRoot" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Row="0" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Column="0">
                <TextBlock VerticalAlignment="Center" FontWeight="Bold" Text="["/>
                <TextBlock VerticalAlignment="Center" FontWeight="Bold" Text="{Binding Locations.CurrentLocation, Mode=TwoWay, Converter={StaticResource LocationNameConverter}, FallbackValue=Current_Location}"/>
                <TextBlock VerticalAlignment="Center" FontWeight="Bold" Text="]"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
                <ListBox Style="{StaticResource BehaviorList}" ItemsSource="{Binding Behaviors}" SelectedValue="{Binding Behavior, Mode=TwoWay}"/> 
            </StackPanel>
        </Grid>

        <Grid x:Name="LayoutMain" Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="0"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="{Binding Image.Loaded, Converter={StaticResource GridLengthConverter}}"/>
            </Grid.ColumnDefinitions>

            <map:Map Name="MyMap" Grid.Row="0" Grid.Column="0" CredentialsProvider="AthZ1tu5ROM0PUWcIYFSxC1oQALFR-g0aoFIuL9tlbeGJ9Z6qKIRYoB_jGpct8Yu">
                <map:MapLayer Name="CornersLayer"/>
                <map:MapLayer>
                    <map:MapItemsControl ItemsSource="{Binding Locations.Buildings}" ItemTemplate="{StaticResource PolygonItemTemplate}" Visibility="{Binding Behavior.ShowBuildings, Mode=TwoWay, Converter={StaticResource VisibilityConverter}}"/>
                    <map:MapItemsControl ItemsSource="{Binding Locations.Corners}" ItemTemplate="{StaticResource CornerPushpinTemplate}"/>
                    <map:MapItemsControl ItemsSource="{Binding Locations.All}" ItemTemplate="{StaticResource PushpinItemTemplate}" Visibility="{Binding Behavior.ShowAllLocations, Mode=TwoWay, Converter={StaticResource VisibilityConverter}}"/>
                    <map:MapItemsControl ItemsSource="{Binding Locations.Top}" ItemTemplate="{StaticResource PushpinItemTemplate}" Visibility="{Binding Behavior.ShowTopLocations, Mode=TwoWay, Converter={StaticResource VisibilityConverter}}"/>
                    <map:MapItemsControl ItemsSource="{Binding Locations.InnerLocations}" ItemTemplate="{StaticResource PushpinItemTemplate}" Visibility="{Binding Behavior.ShowInnerLocations, Mode=TwoWay, Converter={StaticResource VisibilityConverter}}"/>
                </map:MapLayer>
                <controls:DraggablePushpin 
                    Content="{Binding Path=Locations.CurrentLocation, Converter={StaticResource LocationNameConverter}, Mode=TwoWay}"
                    Location="{Binding Locations.CurrentLocation, Converter={StaticResource LocationCenterConverter}, Mode=TwoWay}"/>
            </map:Map>

            <sdk:GridSplitter Grid.Column="1" Width="8" HorizontalAlignment="Left" VerticalAlignment="Stretch" ShowsPreview="True"/>

            <ScrollViewer Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Margin="5,0,0,0">
                <Canvas MouseLeftButtonUp="ImageViewer_Click">
                    <Image Source="{Binding Image.Bitmap}"/>
                    <ItemsControl ItemsSource="{Binding Image.CalibrationPoints, Mode=TwoWay}" ItemTemplate="{StaticResource ImagePinTemplate}"/>
                </Canvas>
            </ScrollViewer>

            <ScrollViewer Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <StackPanel Name="VerticalViewer"/>
            </ScrollViewer>
        </Grid>

        <Grid Grid.Row="1" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <toolkit:Expander Grid.Column="1" Style="{StaticResource ExpanderStyle}" >
                <toolkit:Expander.Header>
                    <TextBlock Text="Tools" Style="{StaticResource ExpanderHeaderStyle}"/>
                </toolkit:Expander.Header>
                <StackPanel HorizontalAlignment="Right">
                    <Button cmds:CommandManager.Command="{Binding GotoRhitCommand}" cmds:CommandManager.CommandEventName="Click" Margin="5" Content="Goto RHIT" HorizontalAlignment="Center"/>


                    <Border BorderThickness="2" BorderBrush="Black" Margin="1">
                        <StackPanel>
                            <StackPanel.Resources>
                                <DataTemplate x:Name="TaskTemplate">
                                    <Button Content="Button" HorizontalAlignment="Center" Margin="5"
                                            DataContext="{StaticResource ViewModel}"
                                            Command="{Binding Behavior.ChangeCornersCommand}"
                                            cmds:CommandManager.Command="{Binding Path=Behavior.ChangeCornersCommand}"
                                            cmds:CommandManager.CommandEventName="Click"/>
                                </DataTemplate>
                            </StackPanel.Resources>
                            <TextBlock x:Name="Penis" Text="Tasks:" FontWeight="Bold" Margin="5,0,0,0"/>
                            <ItemsControl 
                                DataContext="{StaticResource TaskManager}"
                                ItemsSource="{Binding Tasks}" 
                                ItemTemplate="{StaticResource TaskTemplate}"/>
                        </StackPanel>
                    </Border>
                    
                    <!--<Border BorderThickness="2" BorderBrush="Black" Margin="1">
                        <StackPanel DataContext="{StaticResource TaskManager}">
                            <TextBlock Text="Curent Task:" Margin="5,0,0,0"/>
                            <TextBlock Text="{Binding CurrentTask.Label}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <Border BorderThickness="2" BorderBrush="Black" Margin="1">
                        <StackPanel>
                            <TextBlock Text="Tasks:" FontWeight="Bold" Margin="5,0,0,0"/>
                            <Button Content="Sync Floor Plan" Margin="5" HorizontalAlignment="Center"/>
                            <Button Content="Move Building Corners" Margin="5" HorizontalAlignment="Center"/>
                            <Button Content="Add/Remove Corners" Margin="5" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <Border BorderThickness="2" BorderBrush="Black" Margin="1">
                        <StackPanel>
                            <TextBlock Text="Curent Task:" Margin="5,0,0,0"/>
                            <TextBlock Text="Task Name" HorizontalAlignment="Center"/>
                            <TextBlock Text="Step 1"/>
                            <TextBlock Text="Step 2"/>
                            <TextBlock Text="Step 3" FontWeight="Bold"/>
                            <TextBlock Text="Step 4"/>
                        </StackPanel>
                    </Border>-->




                    <!--<Line Margin="0,5" Stroke="DarkGray"  StrokeThickness="2" Stretch="Fill" X1="0" X2="1"/>-->

                    <!--Image Options-->
                    <!--<Button cmds:CommandManager.Command="{Binding Image.LoadCommand}" cmds:CommandManager.CommandEventName="Click" Margin="5,5,5,0" Content="Load Image" HorizontalAlignment="Center"/>

                    <StackPanel Margin="5,15,5,0" Visibility="{Binding Image.Loaded, Converter={StaticResource VisibilityConverter}}">
                        <TextBlock Text="Map/Image Arrangment:" Margin="0,5,5,0"/>
                        <RadioButton Margin="5,5,5,0"  GroupName="ViewArrangment" Content="Horizontal" IsChecked="True"/>
                        <RadioButton Margin="5,5,5,0"  GroupName="ViewArrangment" Content="Vertical"/>
                        <Button cmds:CommandManager.Command="{Binding Image.CloseCommand}" cmds:CommandManager.CommandEventName="Click" Margin="5,5,5,0" Content="Close Image"/>
                    </StackPanel>

                    <Line Margin="0,5" Stroke="DarkGray"  StrokeThickness="2" Stretch="Fill" X1="0" X2="1"/>-->

                    <!--Buildings Options-->
                    <!--<StackPanel Visibility="{Binding ShowLocationsOptions, Converter={StaticResource VisibilityConverter}}">
                        <StackPanel Visibility="{Binding Locations.CurrentLocation, Converter={StaticResource ObjectVisibilityConverter}}">
                            <Button Content="Move Corners" Margin="5" HorizontalAlignment="Center" cmds:CommandManager.Command="{Binding Behavior.ChangeCornersCommand}" cmds:CommandManager.CommandEventName="Click" Visibility="{Binding Behavior.ShowSaveCancel, Converter={StaticResource ReverseVisibilityConverter}}"/>
                            <Button Content="Add/Remove Corners" Margin="5" HorizontalAlignment="Center" cmds:CommandManager.Command="{Binding Behavior.AddCornersCommand}" cmds:CommandManager.CommandEventName="Click" Visibility="{Binding Behavior.ShowSaveCancel, Converter={StaticResource ReverseVisibilityConverter}}"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Save" Margin="5" HorizontalAlignment="Center" cmds:CommandManager.Command="{Binding Behavior.SaveCommand}" cmds:CommandManager.CommandEventName="Click" Visibility="{Binding Behavior.ShowSaveCancel, Converter={StaticResource VisibilityConverter}}"/>
                                <Button Content="Cancel" Margin="5" HorizontalAlignment="Center" cmds:CommandManager.Command="{Binding Behavior.CancelCommand}" cmds:CommandManager.CommandEventName="Click" Visibility="{Binding Behavior.ShowSaveCancel, Converter={StaticResource VisibilityConverter}}"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                    
                    <StackPanel Visibility="{Binding ShowLocationsOptions, Converter={StaticResource VisibilityConverter}}">
                        
                    </StackPanel>-->
                </StackPanel>

            </toolkit:Expander>
        </Grid>
    </Grid>

</navigation:Page>